---
import NextSongsPlayer from "./NextSongsPlayer.astro";
---

<main id="next_songs_container">
  <div id="ns-intro-text-container">
    <h2 id="ns-intro-text">Here’s what i’m willing to learn next.</h2>
  </div>
  <div id="ns-background">
    <div class="bg-rect"></div>
    <div class="bg-rect"></div>
    <div class="bg-rect"></div>
    <div class="bg-rect"></div>
  </div>
  <div id="songs_player">
    <NextSongsPlayer />
  </div>
</main>

<style>
  #next_songs_container {
    width: 100%;
    height: 100dvh;
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
    justify-content: start;
    align-items: start;
    position: relative;
  }

  #ns-intro-text-container {
    background-color: #161616;
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100dvh;
    min-height: 100dvh;
    z-index: 10;
    pointer-events: none;
  }

  #ns-intro-text {
    font-size: clamp(2rem, 4vw + 1rem, 6rem);
    font-weight: bold;
    color: white;
    text-align: center;
    text-wrap: balance;
  }

  #songs_player {
    width: 100%;
    height: 100dvh;
    backdrop-filter: blur(10px);
    background-color: white;
    z-index: 20;
    position: absolute;
    inset: 0;
    opacity: 0;
    pointer-events: none;
  }

  #ns-background {
    position: fixed;
    inset: 0;
    width: 100%;
    height: 100dvh;
    max-height: 100dvh;
    display: flex;
    flex-direction: column;
    justify-content: start;
    align-items: start;
    z-index: 50;
    pointer-events: none;
  }
  #ns-background .bg-rect {
    width: 100%;
    height: 25%;
    background-color: white;
    position: relative;
    scrollbar-width: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  #ns-background .bg-rect::after {
    content: "";
    display: block;
    height: calc(100% / sqrt(2));
    aspect-ratio: 1 / 1;
    transform: translateX(50%) rotate(45deg);
    background-color: white;
  }

  #ns-background .bg-rect::before {
    content: "";
    display: block;
    height: calc(100% / sqrt(2));
    aspect-ratio: 1 / 1;
    transform: translateX(-50%) rotate(45deg);
    background-color: white;
  }

  #ns-background .bg-rect:nth-of-type(2n) {
    transform: translateX(130%);
  }
  #ns-background .bg-rect:nth-of-type(2n + 1) {
    transform: translateX(-130%);
  }
</style>
