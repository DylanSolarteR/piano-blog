---
import { getCollection } from "astro:content";
import NextSongsPlayerClient from "./NextSongsPlayerClient.tsx";

const nextSongsCollection = await getCollection("nextSongsCollection");
const nextSongs = nextSongsCollection.map((entry) => entry.data);
---

<NextSongsPlayerClient nextSongs={nextSongs} client:visible />
